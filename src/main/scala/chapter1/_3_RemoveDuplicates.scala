package chapter1

object _3_RemoveDuplicates {
  /*def removeDuplicates(s: String): String = {
    var n = s.length
    var temp = s
    for (i <- 0 until n - 1) {
      temp = s(i)
      for (j <- i + 1 until n) {
        if (s(i) !ù= s(j)) {
          temp += s.substring(0, j) + s.substring(j + 1, n)
        }
      }
      n = temp.length
    }
  }*/
}
